import { useQuery } from '@tanstack/react-query'
import { Activity, ArrowRight, Gamepad2, Megaphone, Plus, ShieldCheck, Sparkles } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { PageHeader, Section } from '../../components/admin/page'
import { EmptyState, ErrorState, LoadingState } from '../../components/admin/states'
import { StatusBadge, TechnicalLabel } from '../../components/admin/status'
import { Button } from '../../components/ui/button'
import { getJSON } from '../../lib/api'
import { formatDate } from '../../lib/utils'
import { useAuth } from '../auth/auth-context'
import { eventSentence, statusTone } from '../operations/presentation'
import type { WorkbenchSummary } from '../operations/types'

export function WorkbenchPage() {
  const auth = useAuth(); const navigate = useNavigate()
  const query = useQuery({ queryKey: ['workbench', 'summary', auth.state?.identity?.session_version], queryFn: () => getJSON<WorkbenchSummary>('/api/v1/workbench/summary'), refetchInterval: 60_000 })
  if (query.isLoading) return <LoadingState label="正在汇总需要关注的事项…" />
  if (query.error || !query.data) return <ErrorState message={query.error?.message ?? '工作台不可用'} onRetry={() => void query.refetch()} />
  const summary = query.data
  return <div className="grid gap-6"><PageHeader title="工作台" description="今天的异常、待处理事项与最近变化。" eyebrow={`workbench · ${auth.state?.identity?.role ?? ''}`} /><Section title="需要关注" description="由现有持久状态投影，不是新告警系统。">{summary.attention.length === 0 ? <div className="rounded-md border border-success/30 bg-success/5 p-4 text-sm text-success">当前没有对你可见的待处理异常。</div> : <div className="grid gap-3 md:grid-cols-2">{summary.attention.map((item) => <Link key={item.key} to={item.href} className="flex items-center justify-between gap-4 rounded-md border p-4 hover:bg-surface-muted"><div><div className="flex items-center gap-2"><StatusBadge tone={statusTone(item.tone)}>{item.tone}</StatusBadge><p className="font-medium">{item.title}</p></div><p className="mt-2 text-sm text-muted-foreground">{item.summary}</p></div><ArrowRight className="size-4 shrink-0 text-muted-foreground" /></Link>)}</div>}</Section><Section title="快捷操作" description="仅显示当前 principal 具备 capability 的操作。"><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-4">{auth.can('content.write') && <><Button className="h-auto justify-start p-4" onClick={() => navigate('/nav/sites/new')}><Plus className="size-4" />新增网站</Button><Button variant="secondary" className="h-auto justify-start p-4" onClick={() => navigate('/game/games/new')}><Gamepad2 className="size-4" />新增游戏</Button><Button variant="secondary" className="h-auto justify-start p-4" onClick={() => navigate('/nav/update-notices')}><Megaphone className="size-4" />发布公告</Button></>}{auth.can('collection.execute') && <Button variant="secondary" className="h-auto justify-start p-4" onClick={() => navigate('/collection?tab=manual')}><Activity className="size-4" />手动采集</Button>}</div></Section><div className="grid gap-5 xl:grid-cols-2"><Section title="最近变化" actions={<Link className="text-sm font-medium text-primary" to="/changes">全部变化 →</Link>}>{summary.recent_changes.length === 0 ? <EmptyState title="暂无变化" /> : <div className="grid gap-2">{summary.recent_changes.map((change) => <Link key={change.event_key} to={`/changes?domain=${change.domain}`} className="flex items-center gap-3 rounded-md border p-3 hover:bg-surface-muted"><Sparkles className="size-4 text-info" /><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{eventSentence(change.historical_name, change.event_code)}</p><p className="mt-1 text-xs text-muted-foreground">{formatDate(change.event_at ?? change.projection_date)}</p></div><TechnicalLabel>{change.domain}</TechnicalLabel></Link>)}</div>}</Section><Section title="最近操作" description={auth.can('audit.read') ? '历史身份快照。' : '当前账号没有 audit.read。'} actions={auth.can('audit.read') && <Link className="text-sm font-medium text-primary" to="/system/audit">审计日志 →</Link>}>{summary.recent_operations.length === 0 ? <EmptyState title="无可见操作" /> : <div className="grid gap-2">{summary.recent_operations.map((operation) => <div key={operation.id} className="flex items-center gap-3 rounded-md border p-3"><ShieldCheck className="size-4 text-muted-foreground" /><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{operation.operator_name} · {operation.action}</p><p className="mt-1 text-xs text-muted-foreground">{operation.resource} · {formatDate(operation.created_at)}</p></div><StatusBadge>{operation.operator_role}</StatusBadge></div>)}</div>}</Section></div><Section title="系统状态摘要" description="只展示当前 capability 允许的运行与工程信息。"><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{summary.system_status.map((item) => <Link key={item.key} to={item.href} className="rounded-md border p-4 hover:bg-surface-muted"><div className="flex items-center justify-between"><p className="font-medium">{item.label}</p><StatusBadge tone={statusTone(item.status)}>{item.status}</StatusBadge></div><p className="mt-2 text-sm text-muted-foreground">{item.summary}</p></Link>)}</div></Section></div>
}
