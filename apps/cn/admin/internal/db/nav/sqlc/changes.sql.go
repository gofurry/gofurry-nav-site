// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: changes.sql

package navsqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const adminCountNavChangeEvents = `-- name: AdminCountNavChangeEvents :one
SELECT count(*)::bigint FROM gfn_change_events event
WHERE ($1::text='' OR event.detector_key=$1)
  AND ($2::integer=0 OR event.detector_version=$2)
  AND ($3::date IS NULL OR event.projection_date>=$3)
  AND ($4::date IS NULL OR event.projection_date<=$4)
  AND ($5::text='' OR event.event_code=$5)
  AND ($6::text='' OR event.scope_kind=$6)
  AND ($7::text='' OR event.scope_key=$7)
  AND ($8::bigint=0 OR event.site_id=$8)
`

type AdminCountNavChangeEventsParams struct {
	DetectorKey     string      `json:"detector_key"`
	DetectorVersion int32       `json:"detector_version"`
	FromDate        pgtype.Date `json:"from_date"`
	ThroughDate     pgtype.Date `json:"through_date"`
	EventCode       string      `json:"event_code"`
	ScopeKind       string      `json:"scope_kind"`
	ScopeKey        string      `json:"scope_key"`
	EntityID        int64       `json:"entity_id"`
}

func (q *Queries) AdminCountNavChangeEvents(ctx context.Context, arg AdminCountNavChangeEventsParams) (int64, error) {
	row := q.db.QueryRow(ctx, adminCountNavChangeEvents,
		arg.DetectorKey,
		arg.DetectorVersion,
		arg.FromDate,
		arg.ThroughDate,
		arg.EventCode,
		arg.ScopeKind,
		arg.ScopeKey,
		arg.EntityID,
	)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const adminListNavChangeCheckpoints = `-- name: AdminListNavChangeCheckpoints :many
SELECT registry.detector_key, registry.detector_version, registry.status,
       registry.watermark_policy, checkpoint.source_start_date, checkpoint.processed_through,
       CASE WHEN registry.watermark_policy = 'metric_checkpoint_v1' THEN metric.processed_through
            WHEN registry.watermark_policy = 'fact_checkpoint_v1' THEN fact.processed_through
            WHEN registry.watermark_policy = 'closed_day_v1' THEN ((transaction_timestamp() AT TIME ZONE 'UTC')::date - 1)
       END::date AS upstream_processed_through,
       checkpoint.created_at, checkpoint.updated_at
FROM gfn_change_registry registry
JOIN gfn_change_checkpoints checkpoint USING (detector_key, detector_version)
LEFT JOIN gfn_metric_checkpoints metric
  ON metric.metric_key = CASE registry.detector_key WHEN 'ipv6_transition' THEN 'ipv6_adoption' WHEN 'tls13_transition' THEN 'tls13_adoption' WHEN 'security_txt_transition' THEN 'security_txt_adoption' END
 AND metric.metric_version = registry.detector_version
LEFT JOIN gfn_fact_rollup_checkpoints fact
  ON fact.pipeline_key = CASE registry.detector_key WHEN 'tls_certificate_transition' THEN 'nav.target_facts' END
ORDER BY registry.detector_key, registry.detector_version DESC
`

type AdminListNavChangeCheckpointsRow struct {
	DetectorKey              string             `json:"detector_key"`
	DetectorVersion          int32              `json:"detector_version"`
	Status                   string             `json:"status"`
	WatermarkPolicy          string             `json:"watermark_policy"`
	SourceStartDate          pgtype.Date        `json:"source_start_date"`
	ProcessedThrough         pgtype.Date        `json:"processed_through"`
	UpstreamProcessedThrough pgtype.Date        `json:"upstream_processed_through"`
	CreatedAt                pgtype.Timestamptz `json:"created_at"`
	UpdatedAt                pgtype.Timestamptz `json:"updated_at"`
}

func (q *Queries) AdminListNavChangeCheckpoints(ctx context.Context) ([]AdminListNavChangeCheckpointsRow, error) {
	rows, err := q.db.Query(ctx, adminListNavChangeCheckpoints)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AdminListNavChangeCheckpointsRow{}
	for rows.Next() {
		var i AdminListNavChangeCheckpointsRow
		if err := rows.Scan(
			&i.DetectorKey,
			&i.DetectorVersion,
			&i.Status,
			&i.WatermarkPolicy,
			&i.SourceStartDate,
			&i.ProcessedThrough,
			&i.UpstreamProcessedThrough,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const adminListNavChangeEvents = `-- name: AdminListNavChangeEvents :many
SELECT event.event_key, event.detector_key, event.detector_version,
       event.site_id AS entity_id,
       COALESCE(NULLIF(fact.name,''), NULLIF(fact.name_en,''), 'Site #' || event.site_id::text)::text AS historical_name,
       event.projection_date, event.event_at, event.time_basis, event.event_code,
       event.scope_kind, event.scope_key, event.old_value, event.new_value,
       event.source_event_key, event.source_before_key, event.source_after_key,
       event.source_before_at, event.source_after_at, event.source_versions, event.materialized_at
FROM gfn_change_events event
LEFT JOIN gfn_site_daily fact ON fact.site_id=event.site_id AND fact.fact_date=event.projection_date
WHERE ($1::text='' OR event.detector_key=$1)
  AND ($2::integer=0 OR event.detector_version=$2)
  AND ($3::date IS NULL OR event.projection_date>=$3)
  AND ($4::date IS NULL OR event.projection_date<=$4)
  AND ($5::text='' OR event.event_code=$5)
  AND ($6::text='' OR event.scope_kind=$6)
  AND ($7::text='' OR event.scope_key=$7)
  AND ($8::bigint=0 OR event.site_id=$8)
ORDER BY event.projection_date DESC, event.event_at DESC NULLS LAST, event.event_key
LIMIT $10 OFFSET $9
`

type AdminListNavChangeEventsParams struct {
	DetectorKey     string      `json:"detector_key"`
	DetectorVersion int32       `json:"detector_version"`
	FromDate        pgtype.Date `json:"from_date"`
	ThroughDate     pgtype.Date `json:"through_date"`
	EventCode       string      `json:"event_code"`
	ScopeKind       string      `json:"scope_kind"`
	ScopeKey        string      `json:"scope_key"`
	EntityID        int64       `json:"entity_id"`
	RowOffset       int32       `json:"row_offset"`
	RowLimit        int32       `json:"row_limit"`
}

type AdminListNavChangeEventsRow struct {
	EventKey        string             `json:"event_key"`
	DetectorKey     string             `json:"detector_key"`
	DetectorVersion int32              `json:"detector_version"`
	EntityID        int64              `json:"entity_id"`
	HistoricalName  string             `json:"historical_name"`
	ProjectionDate  pgtype.Date        `json:"projection_date"`
	EventAt         pgtype.Timestamptz `json:"event_at"`
	TimeBasis       string             `json:"time_basis"`
	EventCode       string             `json:"event_code"`
	ScopeKind       string             `json:"scope_kind"`
	ScopeKey        string             `json:"scope_key"`
	OldValue        []byte             `json:"old_value"`
	NewValue        []byte             `json:"new_value"`
	SourceEventKey  string             `json:"source_event_key"`
	SourceBeforeKey string             `json:"source_before_key"`
	SourceAfterKey  string             `json:"source_after_key"`
	SourceBeforeAt  pgtype.Timestamptz `json:"source_before_at"`
	SourceAfterAt   pgtype.Timestamptz `json:"source_after_at"`
	SourceVersions  []byte             `json:"source_versions"`
	MaterializedAt  pgtype.Timestamptz `json:"materialized_at"`
}

func (q *Queries) AdminListNavChangeEvents(ctx context.Context, arg AdminListNavChangeEventsParams) ([]AdminListNavChangeEventsRow, error) {
	rows, err := q.db.Query(ctx, adminListNavChangeEvents,
		arg.DetectorKey,
		arg.DetectorVersion,
		arg.FromDate,
		arg.ThroughDate,
		arg.EventCode,
		arg.ScopeKind,
		arg.ScopeKey,
		arg.EntityID,
		arg.RowOffset,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AdminListNavChangeEventsRow{}
	for rows.Next() {
		var i AdminListNavChangeEventsRow
		if err := rows.Scan(
			&i.EventKey,
			&i.DetectorKey,
			&i.DetectorVersion,
			&i.EntityID,
			&i.HistoricalName,
			&i.ProjectionDate,
			&i.EventAt,
			&i.TimeBasis,
			&i.EventCode,
			&i.ScopeKind,
			&i.ScopeKey,
			&i.OldValue,
			&i.NewValue,
			&i.SourceEventKey,
			&i.SourceBeforeKey,
			&i.SourceAfterKey,
			&i.SourceBeforeAt,
			&i.SourceAfterAt,
			&i.SourceVersions,
			&i.MaterializedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const adminListNavChangeRegistry = `-- name: AdminListNavChangeRegistry :many
SELECT detector_key, detector_version, source_kind, source_contracts,
       detection_policy, watermark_policy, event_codes, processing_grain,
       status, description, created_at, retired_at
FROM gfn_change_registry
ORDER BY detector_key, detector_version DESC
`

func (q *Queries) AdminListNavChangeRegistry(ctx context.Context) ([]GfnChangeRegistry, error) {
	rows, err := q.db.Query(ctx, adminListNavChangeRegistry)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GfnChangeRegistry{}
	for rows.Next() {
		var i GfnChangeRegistry
		if err := rows.Scan(
			&i.DetectorKey,
			&i.DetectorVersion,
			&i.SourceKind,
			&i.SourceContracts,
			&i.DetectionPolicy,
			&i.WatermarkPolicy,
			&i.EventCodes,
			&i.ProcessingGrain,
			&i.Status,
			&i.Description,
			&i.CreatedAt,
			&i.RetiredAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const adminNavChangeOverview = `-- name: AdminNavChangeOverview :many
SELECT registry.detector_key, registry.detector_version, registry.status,
       registry.description, registry.watermark_policy,
       checkpoint.source_start_date, checkpoint.processed_through,
       CASE WHEN registry.watermark_policy = 'metric_checkpoint_v1' THEN metric.processed_through
            WHEN registry.watermark_policy = 'fact_checkpoint_v1' THEN fact.processed_through
            WHEN registry.watermark_policy = 'closed_day_v1' THEN ((transaction_timestamp() AT TIME ZONE 'UTC')::date - 1)
       END::date AS upstream_processed_through,
       latest.projection_date AS latest_projection_date,
       COALESCE(latest.event_count, 0)::bigint AS latest_event_count,
       COALESCE(totals.event_count, 0)::bigint AS total_event_count
FROM gfn_change_registry registry
JOIN gfn_change_checkpoints checkpoint USING (detector_key, detector_version)
LEFT JOIN gfn_metric_checkpoints metric
  ON metric.metric_key = CASE registry.detector_key WHEN 'ipv6_transition' THEN 'ipv6_adoption' WHEN 'tls13_transition' THEN 'tls13_adoption' WHEN 'security_txt_transition' THEN 'security_txt_adoption' END
 AND metric.metric_version = registry.detector_version
LEFT JOIN gfn_fact_rollup_checkpoints fact
  ON fact.pipeline_key = CASE registry.detector_key WHEN 'tls_certificate_transition' THEN 'nav.target_facts' END
LEFT JOIN LATERAL (SELECT projection_date, count(*)::bigint AS event_count FROM gfn_change_events event WHERE event.detector_key=registry.detector_key AND event.detector_version=registry.detector_version GROUP BY projection_date ORDER BY projection_date DESC LIMIT 1) latest ON true
LEFT JOIN LATERAL (SELECT count(*)::bigint AS event_count FROM gfn_change_events event WHERE event.detector_key=registry.detector_key AND event.detector_version=registry.detector_version) totals ON true
WHERE registry.status='active'
ORDER BY registry.detector_key
`

type AdminNavChangeOverviewRow struct {
	DetectorKey              string      `json:"detector_key"`
	DetectorVersion          int32       `json:"detector_version"`
	Status                   string      `json:"status"`
	Description              string      `json:"description"`
	WatermarkPolicy          string      `json:"watermark_policy"`
	SourceStartDate          pgtype.Date `json:"source_start_date"`
	ProcessedThrough         pgtype.Date `json:"processed_through"`
	UpstreamProcessedThrough pgtype.Date `json:"upstream_processed_through"`
	LatestProjectionDate     pgtype.Date `json:"latest_projection_date"`
	LatestEventCount         int64       `json:"latest_event_count"`
	TotalEventCount          int64       `json:"total_event_count"`
}

func (q *Queries) AdminNavChangeOverview(ctx context.Context) ([]AdminNavChangeOverviewRow, error) {
	rows, err := q.db.Query(ctx, adminNavChangeOverview)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AdminNavChangeOverviewRow{}
	for rows.Next() {
		var i AdminNavChangeOverviewRow
		if err := rows.Scan(
			&i.DetectorKey,
			&i.DetectorVersion,
			&i.Status,
			&i.Description,
			&i.WatermarkPolicy,
			&i.SourceStartDate,
			&i.ProcessedThrough,
			&i.UpstreamProcessedThrough,
			&i.LatestProjectionDate,
			&i.LatestEventCount,
			&i.TotalEventCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
